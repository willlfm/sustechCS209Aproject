<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.analyzer.mapper.UserMapper">
<!--    <select id="selectBlog" resultType="com.example.analyzer.model.person">-->
<!--        select * from stackoverflow.users-->
<!--    </select>-->
    <insert id="insert" parameterType="com.example.analyzer.model.Person">
        INSERT INTO person (
        user_id,
        reputation,
        accept_rate,
        display_name,
        account_id
        ) VALUES (
        #{userId},
        #{reputation},
        #{acceptRate},
        #{displayName},
        #{accountId}
        )
        ON CONFLICT (user_id)
        DO UPDATE SET
        reputation = EXCLUDED.reputation,
        accept_rate = EXCLUDED.accept_rate,
        display_name = EXCLUDED.display_name,
        account_id = EXCLUDED.account_id
    </insert>

</mapper>